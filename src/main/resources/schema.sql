CREATE TABLE USERS (id BIGINT AUTO_INCREMENT NOT NULL, name VARCHAR(100), dateCreated DATETIME, dateUpdated DATETIME, PRIMARY KEY (id));
CREATE TABLE POSTS (id BIGINT AUTO_INCREMENT NOT NULL, authorId BIGINT, title VARCHAR(200), body CLOB, dateCreated DATETIME, dateUpdated DATETIME, PRIMARY KEY (id), FOREIGN KEY (authorId) REFERENCES USERS);